int x, y, z;
int exmp(void)
{   int a, b, c, d;
    b =  8.7;
	b = d;
	a = b + a;
	d = a * b;
	d = a * 8;

	if(a <b ) {
	    d = a * b;
	} 			
	if(a < 6 ) {
	    d = d;
	} 			
	a = a + 9;

	f( c + a );
	printf("%d",c);
	return 0;
}

