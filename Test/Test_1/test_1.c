int x, y, z;
int exmp(void)
{   int a, b, c, d;
    b = 4;
	a = b + c;
	d = a * b;
    if (x < y)
          b = a -c;
     else
      {   do
          {     c = b + c;
                if (y > x)
              {    do
                     {    d = a + b;
                         f(b + c);
                    } while(y > x);
			  } else
																																								              {    c = a * b;
		              f(a - b);
		          }
																																							                 g (a + b);
		    } while(z > x);
    }
     h(a-c);
     f(b+c);
 }

